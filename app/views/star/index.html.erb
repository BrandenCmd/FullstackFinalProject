<h1 class="title title is-2">Stars</h1>

<%= paginate @stars %>

<div class="columns is-multiline is-mobile">
  <%= render partial: 'star',
             collection: @stars %>
</div>

<%= paginate @stars %>